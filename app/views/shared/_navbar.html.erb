<nav class="navbar navbar-expand-lg navbar-dark bg-dark p-3">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <%= link_to 'UB Brothers', root_path, class: 'navbar-brand'%>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <% if user_signed_in? %>
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item <%= 'active' if current_page?(root_path) %>">
            <%= link_to 'Dashboard', root_path, class: 'nav-link mx-2', aria_current: "page" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Vouchers', root_path, class: 'nav-link mx-2' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Tickets', root_path, class: 'nav-link mx-2' %>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link mx-2 dropdown-toggle rounded-circle d-flex align-items-center" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <div class="avatar-circle me-2">
                <img src="<%= asset_path('avatar.png') %>" alt="User Image" width="40" height="40" class="avatar-img" />
              </div>
              <i class="bi bi-caret-down-fill"></i> <!-- Dropdown arrow icon -->
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <li><%= link_to "Edit Profile", edit_user_registration_path, class: 'dropdown-item' %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete }, class: 'dropdown-item' %></li>
            </ul>
          </li>
        </ul>

      <% else %>
        <ul class="navbar-nav ms-auto ">
          <li class="nav-item">
            <%= link_to 'Register', new_user_registration_path, class: 'nav-link mx-2'%>
          </li>
          <li class="nav-item">
            <%= link_to 'Login', new_user_session_path, class: 'nav-link mx-2'%>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
